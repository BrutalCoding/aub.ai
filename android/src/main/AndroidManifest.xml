<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brutalcoding.aub_ai">

    <!-- Note: This allows the app to use more memory, which is needed to load in big models. -->
    <application
        android:largeHeap="true">
    </application>
</manifest>
